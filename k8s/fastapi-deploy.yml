apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-deployment
  labels:
    app: fastapi-board
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: fastapi-board
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fastapi-board
    spec:
      containers:
        - name: fastapi-container
          image: enohs/fastapi-board:de1063e7e85db70e5b0b26e6b85f292ee4511d47
          ports:
            - containerPort: 8000
          env:
            - name: POSTGRES_HOST
              value: "10.0.2.4"
            - name: POSTGRES_DB
              value: "cloudnative"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "mysecretpassword"
