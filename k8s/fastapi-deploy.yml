apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-deployment
  labels:
    app: fastapi-board
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: fastapi-board
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fastapi-board
    spec:
      containers:
        - name: fastapi-container
          image: enohs/fastapi-board:22ac5eb4b3badbbb1244fe7540d2a6a4e66827b0
          ports:
            - containerPort: 8000
          env:
            - name: POSTGRES_HOST
              value: "10.0.2.4"
            - name: POSTGRES_DB
              value: "cloudnative"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "mysecretpassword"
